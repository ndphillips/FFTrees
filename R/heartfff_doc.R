#' An FFForest object trained on the heartdisease dataset.
#'
#'
#' @format An FFForest object
#' @details The code used to generate this object was \code{heart.fff <- FFForest(diagnosis ~., data = heartdisease, ntree = 100, train.p = .5, cpus = 4)}
#'
#'

"heart.fff"
